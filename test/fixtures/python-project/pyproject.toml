[project]
name = "my-python-tool"
version = "0.3.1"
description = "A Python CLI tool"
requires-python = ">=3.9"

[project.urls]
Homepage = "https://github.com/testuser/my-python-tool"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
